spring:
  datasource:
    url: jdbc:tc:mysql:8.0.36:///db?user=root?password=?TC_MY_CNF=mysql_conf_override
    username: root
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration